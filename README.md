untuk update database, ambil dari program maxypos, master > tabel product, export taruh di file (test1.xls),
buka file test1.xls, select all > format cell > general, lalu save ke pakai format .csv di listDamaiFull.csv
ke website github, upload file listDamaiFull.csv

untuk promo bisa di edit di github, bisa di upload
untuk tanggal update mengikuti tanggal di promo.csv github

untuk stok ambil dari maxypost, menu laporan > posisi stok > export, ke file test2.xls,
buka file test2.xls, save dalam format .csv ke stok.csv, lalu upload di github

